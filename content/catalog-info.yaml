apiVersion: backstage.io/v1alpha1
kind: Resource
metadata:
  name: ${{ values.name | dump }}
  annotations:
    backstage.io/techdocs-ref: dir:.
  tags:
    - cnp
    - config
    - yaml
spec:
  type: cluster-config
  owner: user:mbowen
  system: cnp
